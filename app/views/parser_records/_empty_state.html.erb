<div class="card text-center">
  <div class="card-body py-5">
    <i class="bi bi-inbox display-1 text-muted"></i>
    <h3 class="mt-3"><%= t('parser_records.index.empty.title') %></h3>
    <p class="text-muted"><%= t('parser_records.index.empty.message') %></p>
    <%= link_to new_email_path, class: "btn btn-primary mt-3" do %>
      <i class="bi bi-upload"></i> <%= t('parser_records.index.empty.upload_button') %>
    <% end %>
  </div>
</div>
